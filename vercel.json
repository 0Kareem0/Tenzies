{
  "buildCommand": "cd project-00021 && npm install && npm run build",
  "outputDirectory": "project-00021/dist",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}
