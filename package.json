{
  "name": "tenzies-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd project-00021 && npm install && npm run build",
    "dev": "cd project-00021 && npm run dev"
  }
}
